# 제뉴인학원 학생 앱 — 개발 요구사항 정리

이번 빌드 범위: **학생용 앱만** (반별 출석부/선생님 앱은 제외, 별도 트랙으로 진행)
배포 대상: **iOS 앱스토어 + 구글 플레이스토어** (다운로드 후 실제 사용 가능한 앱)

---

## 1. 사용자 유형

- **학생**: 모바일 앱 사용자
- **관리자**: 회원가입 승인/박탈, 단어·숙제 콘텐츠 업로드 담당. 학생 앱과는 별개로 **관리자용 화면(웹 대시보드 형태 추천)** 이 필요함

## 2. 계정 상태와 흐름

```
회원가입 → [대기 pending] → 관리자 승인 → [승인 approved] → 정상 이용
                                              ↓ (학원 탈퇴 시)
                                         [박탈 revoked] → 이용 불가
```

- **대기(pending)**: 회원가입 직후 상태. 로그인은 되지만 "승인 대기 중입니다" 화면만 보이고 그 외 기능은 접근 불가
- **승인(approved)**: 관리자가 승인 처리한 상태. 홈/단어/숙제 전체 기능 정상 이용
- **박탈(revoked)**: 학원을 그만둔 학생. 로그인은 되더라도 "이용이 종료되었습니다" 화면만 보이고 기능 접근 불가

### 구현 시 반드시 지켜야 할 점

앱을 이미 설치해서 로그인해 둔 학생이라도, 관리자가 상태를 바꾸면 **다음 화면 진입이나 API 요청 시점에 즉시 반영**되어야 함. 클라이언트에 저장된 로그인 토큰만 믿고 상태를 캐싱하면 안 되고, 매 요청마다(혹은 최소 앱 실행 시마다) 서버에서 현재 상태를 확인해야 함.

숙제 제출도 마찬가지로, **"다시 풀기" 버튼을 앱 화면에서 없애는 것만으로는 부족함**. 정답이 이미 공개된 상태에서 학생이 답을 바꿔 재제출하면 채점 의미가 없어지므로, 서버(백엔드) 쪽에서도 이미 제출된(submitted=true) 과제에 대한 답안 수정/재제출 요청은 거부하도록 막아야 함.

## 3. 회원가입 (확정)

- 입력 항목은 **이름 + 전화번호, 이 두 가지뿐**. SMS 인증번호 같은 별도 본인인증 절차는 두지 않음
- 승인은 자동이 아니라 **관리자가 직접 확인**하는 방식: 승인 대기 목록에 이름·전화번호가 뜨면, 관리자가 본인이 알고 있는 재원생 명단과 대조해서 맞으면 승인, 모르는 이름이거나 이상하면 반려
- 참고: 같은 학생이 앱을 재설치하거나 기기를 바꿔서 같은 이름+전화번호로 다시 가입 신청을 하면 중복 계정이 생길 수 있으므로, 이미 승인된 전화번호로 재가입 시 새 대기 건을 만들지 않고 기존 계정에 연결하는 처리를 넣는 게 좋음 (클로드 코드에 구현 시 참고)

## 4. 관리자가 업로드/처리하는 것

- **반 배정(확정)**: 학생은 가입 시 반을 선택하지 않음. 승인 대기 목록에서 관리자가 이름·전화번호로 학생을 확인하면서 **반을 직접 지정**하고, 반이 지정되지 않으면 승인 버튼이 비활성화됨. 이미 승인된 학생의 반도 관리자가 언제든 변경 가능
- **회원 승인**: 대기 목록에서 이름·전화번호를 보고 반을 지정한 뒤 승인 또는 반려
- **회원 상태 관리**: 재원 중인 학생을 박탈 상태로 전환 (학원 탈퇴 시)
- **단어**: 반별 또는 학생별로 주차 단어 목록 업로드(엑셀/CSV) → 앱이 자동으로 요일별로 나눠서 배분
- **숙제**: 문제 수 + 정답 키(객관식) 입력 → 학생 앱에 과제로 노출
- **일괄 배정(확정)**: 단어/숙제 업로드 화면에서 학생 목록을 체크박스로 여러 명 선택한 뒤, **업로드는 한 번만** 하면 선택된 모든 학생에게 한꺼번에 배정되어야 함. 반 전체를 한 번에 체크하는 옵션도 포함

## 5. 학생 앱 화면 구성

### 기존에 설계 완료된 화면 (그대로 유지)
- 홈: 이번 주 성취도 원형 그래프 (단어 암기 + 제출한 숙제 개수 합산)
- 단어 탭: 외우기(일자별 플래시카드) / 단어시험 제출(사진 촬영 업로드 → 조교 채점 대기)
- 숙제 탭: 과제 목록 → 개별 과제 진입 → 객관식 체크 → 제출 시 자동채점. **제출은 1회로 제한** — 제출 후에는 재제출/다시풀기가 불가능하며, 채점 결과 화면만 보임 (프로토타입에서 "다시 풀기" 버튼 제거함)
- 공지사항 탭: 관리자가 게시한 공지를 목록으로 확인, 안 읽은 공지는 표시가 남음

### 이번에 추가로 필요한 화면
- 회원가입 화면
- 승인 대기 화면 (기능 접근 차단)
- 이용 종료 화면 (박탈 상태, 기능 접근 차단)
- 로그인 화면 (이름 + 전화번호 입력 — 별도 인증번호 없이, 서버에 저장된 값과 일치하면 로그인)

## 6. 데이터 구조 (초안)

- `students` — 이름, 전화번호, **class_id**(가입 시엔 비어있음, 관리자가 승인 과정에서 지정), **status**(pending / approved / revoked), 가입일, 승인일, **report_token**(학부모 리포트 링크용 고유 토큰, 승인 시 자동 생성)
- `admins` — 관리자 계정
- `classes` — 반 정보
- `notices` — 관리자가 게시한 공지(제목, 내용, 게시일) — 전체 학생에게 노출되는 공용 콘텐츠
- `attendance_records` — student_id + 날짜 (관리자가 출석 체크할 때마다 한 건씩 생성)

콘텐츠(단어/숙제)는 **한 번만 저장**하고, 학생과의 연결은 **별도 배정 테이블**로 관리 (여러 학생을 체크해서 한 번에 업로드해도 콘텐츠는 하나만 생기고, 배정 연결만 학생 수만큼 생기는 구조):

- `vocab_sets` — 업로드 배치 단위(예: "5주차 단어"), 생성일
- `vocab_words` — 개별 단어, vocab_set_id 참조, 요일(1~7) 자동 배정
- `vocab_assignments` — vocab_set_id + student_id (이 단어세트를 이 학생에게 배정했다는 연결)
- `vocab_progress` — student_id + word_id + 암기 여부 + 날짜 (배정 시점에 학생별로 생성)
- `homework_assignments` — 과제 정의(제목, 문제 수, 정답 키) — 한 번만 저장되는 콘텐츠
- `homework_student_assignments` — homework_assignment_id + student_id (이 과제를 이 학생에게 배정했다는 연결)
- `homework_submissions` — student_id + homework_assignment_id + 제출 답안 + 채점 결과
- `vocab_test_submissions` — 사진 URL, 채점 대기/완료 상태, 조교 점수

## 7. 추천 기술 스택

- **클라이언트**: **일반 React 웹 앱 + PWA로 결정.** 선생님용 출석 앱("제뉴인")은 선생님 한 명이 안드로이드 폰 하나로만 쓰기 때문에 Expo APK로 충분하지만, 학생 앱은 여러 학생이 아이폰·안드로이드가 섞인 상태로 쓰게 되므로 APK(안드로이드 전용)나 Expo 네이티브 빌드가 아니라 **PWA**가 맞음. 기존에 만든 프로토타입 3개(student/admin/parent report)가 이미 웹 코드라 그대로 이식 가능 — Expo로 다시 짤 필요 없음
  - 나중에 앱스토어 정식 출시가 필요해지면, 같은 웹 코드를 Capacitor로 감싸서 올리는 것도 가능 (다시 짤 필요 없이 확장 가능)
- **백엔드**: Supabase (Auth + DB + Storage)
  - Row Level Security로 "학생은 자기 데이터만 조회/수정 가능" + "status가 approved인 학생만 데이터 접근 가능" 규칙을 DB 레벨에서 강제하는 것을 추천 (클라이언트 코드 실수로 우회되지 않도록)
- **관리자 대시보드**: 같은 Supabase를 쓰는 별도의 간단한 웹 화면 (모바일 앱과 분리)

## 8. 관리자 도구를 별도 트랙으로 만들 때 연동 방법

관리자 도구는 **앱스토어에 올라가는 앱이 아니라 웹 대시보드로 확정**. 브라우저로 접속하는 형태라 배포도 빠르고 심사 과정도 없음.

관리자 도구와 학생 앱은 서로 직접 통신하지 않음. **같은 Supabase 프로젝트를 공유**하는 것이 연동의 전부이며, 관리자가 DB 값을 바꾸면 학생 앱은 다음 확인 시점에 그 값을 그대로 읽어감.

- 관리자 도구용으로 **새 Supabase 프로젝트를 만들지 말고, 학생 앱과 동일한 프로젝트 URL/키를 사용**할 것
- 이 문서의 6번(데이터 구조)을 관리자 도구 쪽 클로드 코드 세션에도 그대로 전달해서 같은 테이블·컬럼 이름으로 작업할 것
- `admins` 테이블(또는 role 구분) + Row Level Security로 "관리자만 학생 status 변경·단어/숙제 업로드 가능"을 DB 레벨에서 강제할 것 (클라이언트 코드 버그로 우회되지 않도록)
- 승인/박탈처럼 RLS를 우회해야 하는 작업은 `service_role` 키를 관리자 도구 서버 쪽에서만 사용하고, 학생 앱 코드에는 절대 포함하지 말 것
- 웹 대시보드는 일반 React/Next.js 웹 프로젝트로 만들면 됨 (Expo/네이티브 빌드 도구 불필요 — 그건 앱스토어 배포용 학생 앱에만 해당). Vercel 같은 곳에 올리면 배포도 간단함

### 임시 대안 (웹 대시보드 완성 전)

학생 앱을 먼저 출시하고 관리자 도구는 나중에 만들 계획이라, 그 사이 기간에는 **Supabase가 기본 제공하는 Table Editor**(스프레드시트 같은 화면)로 학생 status를 직접 바꾸거나 단어/숙제 데이터를 입력해도 됨. 별도 코드 없이 Supabase 프로젝트 대시보드에 로그인해서 바로 쓸 수 있어서, 커스텀 관리자 화면이 나오기 전 공백을 메우기 좋음.

## 9. 이번에 추가된 기능

- **출석 체크(관리자 대시보드)**: 관리자가 학생별로 "오늘 출석 체크" 버튼을 누르면 그 날짜가 기록됨. 하루에 중복으로 체크되지 않도록 이미 체크한 날은 버튼이 "체크완료"로 바뀌고 비활성화됨. 결제/8회차 주기와는 연결되지 않은 단순 출석 기록임 (그 기능은 별도 트랙인 반별 출석부 쪽 개념)
- **공지사항**: 관리자 대시보드에서 제목+내용으로 작성해 게시하면 모든 학생 앱에 바로 노출됨. 현재는 전체 공지만 지원 (반별/학생별 타겟팅은 필요해지면 추가)
- **학부모 리포트(매직 링크)**: 로그인 없이, 학생마다 고유하고 예측 불가능한 토큰이 붙은 링크(`?key=토큰`)로 접속하면 그 학생의 출석 기록·단어시험 점수·숙제 점수를 바로 볼 수 있음. 토큰은 학생 승인 시 서버에서 자동 생성해 저장하고, 관리자 대시보드의 "리포트 링크 복사" 버튼으로 복사해서 학부모에게 카톡 등으로 직접 전달. 토큰은 이름/전화번호처럼 예측 가능한 값이 아니라 충분히 길고 무작위인 값으로 생성해야 함 (안 그러면 다른 사람이 URL을 추측해 접근할 위험이 있음). 화면 프로토타입 완성 — 이번 달 출석일수, 단어시험 점수 이력(채점 대기 포함), 숙제별 점수를 한 페이지에서 확인
- **숙제 QR 코드**: 관리자가 숙제를 만들면 그 숙제 전용 QR 코드가 함께 생성됨. 이 QR을 종이 워크시트에 인쇄해서 나눠주고, 학생이 폰 카메라로 스캔하면 **그 숙제의 답안 체크 화면이 바로 열림** (반 전체가 같은 워크시트를 받으므로 QR은 학생별이 아니라 **숙제 하나당 하나**면 됨 — 열리는 순간 로그인된 학생 본인의 답안지가 뜨는 구조).
  - QR은 숙제별 고유 URL(예: `academy.app/hw/{숙제ID}`)을 인코딩한 것. 관리자 대시보드에서 미리보기/다운로드 가능해야 함
  - **실제 구현 시 중요한 부분**: 이 URL을 스캔했을 때 브라우저가 아니라 **설치된 앱이 바로 열리게 하려면 Universal Links(iOS) / App Links(Android) 설정이 필요**함. 도메인 소유를 증명하는 파일(iOS는 `apple-app-site-association`, Android는 Digital Asset Links)을 서버에 올려두고 앱과 연결해야 함 — 이건 Capacitor 문서에 표준으로 나와 있는 절차라 클로드 코드에서 무리 없이 처리 가능함
  - 앱은 이 링크로 열렸을 때 "숙제 탭 → 해당 숙제 상세 화면"으로 바로 진입하도록 처리해야 함 (지금 프로토타입의 `hwView` 상태를 열림과 동시에 세팅하는 것과 같은 개념)
  - 프로토타입의 QR 이미지는 데모용으로 외부 QR 생성 API(api.qrserver.com)를 사용했음. **실제 서비스에서는 외부 서비스에 의존하지 않도록 `qrcode` 같은 라이브러리로 서버/클라이언트에서 직접 생성하는 것을 추천** (외부 서비스 장애·URL 유출 위험 방지)

## 10. 앱스토어 심사 관련 참고

- "회원가입 → 승인 대기" 구조는 애플/구글 심사 시 리뷰어가 바로 못 들어가 볼 수 있으므로, **심사용으로 미리 승인된 테스트 계정**을 만들어 리뷰 노트에 안내해야 심사가 매끄러움
- 계정 박탈 기능이 있다는 것도 리뷰 노트에 간단히 설명해두면 좋음 (사용자 데이터 삭제/차단 정책 관련 질문을 줄일 수 있음)
